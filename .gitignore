# binaries
/excise
/dist/
*.out
*.test

# OS
.DS_Store
Thumbs.db

# editor
.idea/
.vscode/
*.swp

# claude
CLAUDE.md
.claude/

# coverage
coverage.txt
*.cov

# Local-only build-pipeline metadata (never commit)
.build_metadata*.yaml
.builder_prompt*.md
.spawn.*.log

# build scratch (internal tooling — never commit)
.spawn.*
.builder_*
.build_metadata*
.stage_*
PUBLISH_BLOCKED.md
BUILD_SETUP_NEXT_STEPS.md
DRIFT_REPORT.md
GIT_AUTHOR_DRIFT.md
